{
  "name": "stov-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "STOV AI Scientist secure gateway — browser/backend boundary for the LangGraph agent server",
  "type": "module",
  "scripts": {
    "dev": "tsx src/dev.ts",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "deploy:check": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "@hono/node-server": "^1.13.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.90.0"
  }
}
